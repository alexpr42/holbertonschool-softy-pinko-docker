# Use the latest official Ubuntu image as the base
FROM ubuntu:latest

# Update the package list and software
RUN apt-get update && apt-get upgrade -y

# Command to run when the container starts
CMD echo "Hello, World!"
